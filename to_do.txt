# To Do

======

## Frontend

- Implement Frontend Editing on Clip detail page. Or course, check user permissions, and only display frontend edit link if Frontend Editing is enabled in Joomla configuration.

- Audio archive and tag directory menu items need an additional free text setting, so we can have some optional introductory text displayed above the filter box or above the tag directory.

---

## Backend

### Dashboard

- Text ("Audio Archive status Manage clips, upload...") should be updated.

- In "System Check > Storage paths", it still says "Waveform storage". But should be something liek "Analysis data storage".

### Integrity & Maintenance page

- Loads very slow, probably because of those checks/scans made on page load. Already takes some seconds for my 816 clips, but will get even slower with more. Maybe don't make any checks, but instead have "Check" buttons trigger the checks manually?

#### Waveforms

- We have "Queue missing waveforms", "Queue state waveforms", "Retry failed Waveforms". We need a way to regenerate all waveforms.

- We also need a ways to delete all waveform data.

#### Spectral analyses

- Wave have "Queue missing spectral analyses", "Queue stale spectral analyses", "Retry failed spectral analyses" and "Regenerate all spectral analyses". The first ones use the queue, where a list is displayed, the "Regenerate" button just shows its own progerss bar and no list.

- We also need a ways to delete all spectral analysis.

## Frontend

### Audio Archive Module

- Add more selection modes: Longest and Shortest will show the x longest or shortest clips in the archive.

### Filter

The current filter settings and archive list sorting settings should be stored in the user's session (does this work for guests / unregistered users?). I want to last used filter and sorting settings to be used whenever I return to the archive page.

---

FFmpeg & FFprobe
- Add frequency analysis

---

Content Plugin
- Add another mode: {audioarchive longest} and {audioarchive shortest} which will show the longest or shortest elegible clip in the archive.

Frontend
- Add logical "OR" mode to tags filter
- Add little "Clear tag filter" link or button or icon to the tags filter list

---

Backend

Now it's time to create two groups in Components >> Audio Archive >> Processing!
The first group "Clip analysis" should contain "Enable waveform generation", "Waveform detail", "Queue waveform after upload".
The second group "Processing" should contain "Explicit FFmpeg path", "Explicit FFprobe path", "External process timeout in seconds" and "Maximum processing attempts".

Content Plugin
- Settings in the backend (Backend >> System >> Plugins >> Content - Audio Archive): The switches for the options are reversed: Yes is left, No is right. Those options/switches should work like in the global settings: For options named "Show (something)" the switch should be Hide (left) and Show (right).

---

FFmpeg and FFprobe
- I can enter the paths to FFmpeg and FFprobe in the global configuration, but are they ever used? I have put FFmpeg and FFprobe in (joomla_root)/audioarchive/ffbin, and therefore entered the paths "audioarchive/ffbin/ffmpeg" and "autioarchive/ffbin/ffprobe". But the Dashboard still reports them as missing / not found.

---

Build python script
- Since we now have a folder "language" in the package, we should take care the build script does not assume it's another nested extension archive and zip it. How can we do that? Skip folders that are called "language"? Or handle folders as extensions that atart with "com_", "plg_", "mod_" and so on? Or handle folders as extensions that have a "_" in their name?

---

- If the current user has the permissions to edit clips, an edit link on a clip's detail page would be nice. If it's too cumbersome to implement frontend editing, it's okay if the link simply points to the clip edit page in the backend.

---

Package

- In pkg_audioarchive.xml, move the text from <description> to the language files. Then use some HTML tags to make it look better (e.g. a heading, an unordered list for the included features, et cetera)

---

Backend

System >> Global configuration >> Audio Archive >> Upload and Import
- Introduce sub groups here, too, as you did in "Public Archive". "Permitted file extensions", "Permitted MIME types", "Maximum file size in MB" and "Maximum duration in seconds" should be grouped in a group named "General", and "Delete inbox after successful import", "Exact duplicate policy", "Automatic title policy" and "Recoding-date policy" go into a group named "Import".

Dashboard
- There are several buttons (Manage Clips, Bulk Upload, Directory Import, Integrity & Maintenance, Manage Categories, Smart Search Index, Options)... can we group them a bit more intuitively? One group would be "Manage Clips", "Manage categories", "Bulk Upload", "Directory Import". The next group would be "Integrity & Maintenance", "Smart Search Index" (only if "plg_finder_audioarchive" is installed, of course), "Options".

Upload
- When Dark Mode is used, the Buttons "Select Files", "Start Upload" and "Clear Finished" aren't really visible, and look like they were disabled. In Light Mode, it looks fine.

Import
- When Dark Mode is used, the Buttons "Scan Inbox", "Select Elegible", "Deselect All" and "Import Selected" aren't really visible, and look like they were disabled. In Light Mode, it looks fine.

Integrity & Maintenance
- All fine here

Categories
- All fine here

Sorting of items in the sidebar need to be changed:
- Dashboard
- Clips
- Categories
- Upload (rename to "Bulk Upload")
- Import (rename to "Directory Import")
- Integrity & Maintenance

---

- Active Filter Summary: Would be cool if we could click on one of those items to have that filter removed

- Bug: Clicking on any of the pagination links at the bottom of the archive page will omit the curently filters. For example, I am on "https://www.athousandfarts.com/archive?tags=bassy&duration_max=0:12", and the link to page 4 is "https://www.athousandfarts.com/archive?start=60"

---

- Bug: Filter: Setting any filter will also add a Maximum duration filter set to maximum duration time (so it doesn't do anything)
- Bug: URL with set filter contains "&amp;" instead of "&"

---

Backend

Tab "General"

- Remove "Frontend archive access" and move it to "Public Archive"
- Keep "Default category", "Default access level", "Default publication state"
- Do "Default archive ordering", "Default sort direction" and "Default items per page" apply to the frontend public archive? If so, remove those settings from this "General" and move them to "Public Archive".
- Keep "Enable playback counts" and "Enable download counts"

Tab "Storage"

- Keep all settings here as they are now

Tab "Upload and Import"

- Keep all settings here as they are now

Tab "Public Archive"

We need some section headings here.

Section "General"

- "Frontend archive access" (moved here from tab "General")

Section "Archive filter"

- "Filter Panel default state"
- These settings go here: "Show text search", "Show category filter", "Show tag filter", "Show duration filter", "Show recording-date filter", "Show upload-date filter", "Show active-filter summary"
- "Show empty categories" (right after "Show category filter")

Section "Archive list"

- If we moved "Default archive ordering", "Default sort direction" and "Default items per page" frmo tab "General", put them here.
- "Show result count"

Section "List columns"

- From tab "Archive columns" take all settings and move them here: "Play", "Title", "Category", "Duration", "Recording date", "Upload date", "Tags"

Section "Pagination"

- "Show pagination", "Show page-size selector", "Allowed page sizes", "Maximum page size"

Tab "Archive Columns"

- Remove this tab completely, as we have moved all its settings to tab "Public Archive" >> section "List columns"

Tab "Playback and Downloads"

- Keep all settings here as they are now

Tab "Clip Detail"

- Keep all settings here as they are now

Tab "Processing"

- Keep all settings here as they are now
- Add some help text for the "Explicit FFmpeg path" and "Explicit FFprobe path" settings. I don't know whether a path relative to the Joomla root, or an absoluet server path must be used, and whether the name of the binary has to be included or not.

Tab "Uninstallation"

- Keep all settings here as they are now

Tab "Permissions"

- Keep all settings here as they are now
